#include <stdio.h>
#include "ArrayList.h"

int main(int argc, char const *argv[]){
    int size = 5;
    struct ArrayList * list = arraylist_create(size);

    printf("my firs array");
    return 0;
}